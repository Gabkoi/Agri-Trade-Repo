import { StyleSheet, Text, View, Button} from 'react-native'
import React, {useState} from 'react'

export default function Home() {
  const [name, setName] = useState("shui")

  const clickHandler = () =>{
    setName("Akoi GAbrie")
  }

  return (
    <View style={styles.container}>
      <Text>my name is {name}</Text>
      <Text></Text>

      <View style={styles.Button}>
        <Button title='update State' onPress={clickHandler}/>
      </View>
    </View>
  )
}






const styles = StyleSheet.create({
  container:{
    flex:1,
    justifyContent: "center", 
    alignItems: "center",
  },

  Button:{
    marginTop: 20

  }
  
});


    // <View>
    //   <Text>
    //     I am {props.name}, and I am {isHungry ? 'hungry' : 'full'}!
    //   </Text>
    //   <Button
    //     onPress={() => {
    //       setIsHungry(false);
    //     }}
    //     disabled={isHungry}
    //     title={isHungry ? 'Pour me some milk, please!' : 'Thank you'}
    // //   />
    // </View>



const Cat = props => {
  // const[isHungry , setIsHungry] = useState(true);
  const[text , setText] = useState("")


  return (
    <View style={{padding: 10}}>
      <TextInput placeholder="Enter your text for tranlate" 
      onChange={newText => setText(newText)}
      defaultValue="text"
      >
      </TextInput>
    </View>
    

  );
};

export default Cat



const increament = () =>(
    setCount(count + 1)
  );

  const decreament = () =>{
    setCount(count - 1)
  } 


  return(
    <View>
      <Text>This is the number{count}</Text>
      <Button title="increase"onPress={increament}/>
      <Button title="decrease" onPress={decreament} />


      </View>

  );
  
};
